# Fix .env Configuration

## Tasks
- [x] Fix `backend/.env.example` — replace incorrect `AI_API_KEY`/`AI_MODEL` with the correct `GROQ_API_KEY`/`GROQ_MODEL` names
- [x] Create `backend/.env` with corrected values so the API works immediately
- [x] Verify the config by running the backend

